# Eigene Läufe bleiben lokal — nur output/.gitkeep wird getrackt
output/*
!output/.gitkeep

# Keine YAML-Dateien im Wurzelverzeichnis (lokale Cases) — Eingaben liegen in input/
/*.yaml

# OS-/Scratch-Dateien
.DS_Store
.claude/worktrees/
.claude/settings.local.json
